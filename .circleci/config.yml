version: 2.1

jobs:
  # create_infrastructure:
  #   docker:
  #     - image: amazon/aws-cli
  #   steps:
  #     - checkout
  #     - run:
  #         name: Ensure backend infrastructure exist
  #         command: |
  #           aws cloudformation deploy \
  #             --template-file template.yml \
  #             --stack-name my-stack

   configure_infrastructure:
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints: ["3d:aa:cb:cb:96:7f:b3:16:d3:69:4b:3e:98:72:72:f4"] # You can get this ID in the section where you registered the SSH Key
      - run:
          name: Install dependencies
          command: |
            apk add --update ansible # install the dependencies needed for your playbook
      - run:
          name: Configure server
          command: |
            ansible-playbook -i inventory main-remote.yml
   smoke_test:
     docker:
       - image: ubuntu
     steps:
       - run:
          name: Smoke_test_by_curl
          command: |
            if curl -s --head "https://52.90.177.57:3000/" ; then   exit 0; else  exit 1; fi
            
workflows:
  my_workflow:
    jobs:
      #- create_infrastructure
      - configure_infrastructure
      - smoke_test